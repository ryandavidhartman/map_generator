# Hex Zoom-In: Sites, Settlements, Encounters & Multi-Campaign Mongo Persistence

> **Durable copy.** This is a copy of the working plan originally saved to
> `~/.claude/plans/bright-watching-wolf.md` (a local, non-git-tracked path —
> plan files there don't survive a fresh clone or a different machine).
> Copied into the repo so it travels with `git clone`/`git pull` instead of
> depending on local Claude Code state. See `CLAUDE.md` for current status;
> update both when this plan changes.
>
> **Implementation note:** the "UI / navigation" section below sketches the
> *original* routing design (`/campaigns/new`, `/campaigns/:campaignId`,
> `/campaigns/:campaignId/hex/:hexId`, a `CampaignListPage`/`CampaignMapPage`
> split) written before Phase 2 was built. What actually shipped in Phase 2
> is simpler: just `/` (overland map) and `/hex/:hexId` (full view) — no
> campaign routing yet, since that's genuinely Phase 10 (Mongo, renumbered
> twice as more phases were inserted before it — see Phasing below)
> work. Treat the original routes sketch as directional intent for Phase 10,
> not as something already in the codebase.

## Context

The hex-crawl generator currently does one thing well: overland map generation, one hex at a time, persisted to a single browser-localStorage slot. The user wants the natural next layer: click into any revealed hex to see a dedicated "full view" for it, where a Point of Interest hex generates either a settlement (districts, taverns, shops) or a dungeon site (rooms, contents, objective room) using the corresponding Shadowdark book tables, laid out on a small procedurally-shaped grid. Any revealed hex should also support rolling a random encounter from its terrain's table. Because each overland map now represents an ongoing campaign for a specific party, and a GM will run multiple campaigns over time, this needs upgrading from single-slot localStorage to a real multi-campaign store — a small Node/Express + MongoDB backend.

**Update:** Phases 1-4 below are done and browser-verified. Before starting the Mongo persistence work, the user raised requirements and set the build order across two rounds:
- Round 1: support arbitrary-size hex maps (today's map is bounded to a fixed `radius` chosen at start) — sequenced *before* Mongo so the campaign schema never has to carry, then later drop, a `radius` field. Done (Phase 4).
- Round 2 (2026-07-04): the dungeon/settlement generators need to produce **real, detailed maps** (not the abstract grid-blob cell layout used today) and **populate them with actual generated monster/NPC entities**, not just book-table flavor text. This is confirmed firm scope, not speculative polish. A sibling Scala project this user also maintains, `~/dev/source/shadowdark-rest`, already implements this at a much higher fidelity (BSP-style dungeon room layouts, Voronoi-district organic settlements with smoothed boundaries and curved road networks, NPC population wired to settlement POIs) — the intent is to reuse those *algorithms/data-model ideas* (not literal code; different language/stack) rather than design from scratch. This split into four new phases (dungeon maps, dungeon population, settlement maps, settlement population), all sequenced *before* Mongo for the same schema-stability reason as arbitrary-size maps — see the four new phase sections below. Scope explicitly confirmed as dungeons + settlements only for now (not overland random encounters). Neighbor-weighted terrain generation (Ocean coherence) stays *after* Mongo, unchanged from Round 1's reasoning, since it's a self-contained `generateHex.ts` change with no schema implications.

See the "Real dungeon maps", "Dungeon monster/NPC population", "Real settlement maps", "Location Generator expansion", "Settlement NPC population", "Arbitrary-size hex maps", and "Neighbor-weighted terrain generation" sections and the updated Phasing list below.

**Update (2026-07-04, same day as the settlement-map revision):** the user identified a thematic gap — only 4 dungeon Site Types (Cave/Tomb/Deep tunnels/Ruins) exist despite 20+ distinct overland Points of Interest flavors, so most POIs resolve to a mechanically-identical dungeon with no thematic connection to their flavor text. This grew into a confirmed new house-rule expansion, inserted here as **Phase 8**, before Settlement NPC population and Mongo — see "Location Generator expansion" below for the full design and current status (all 5 new site kinds — Tower/Shrine/Rift/Keep/Camp — done and browser-verified; only the batched POI table cutover remains).

All book tables for this work (Shadowdark Maps p.130-131, Overland Hex Maps p.132-133 [already implemented], Settlement Maps p.134-139, and the full Random Encounters appendix p.142-185) were read directly from the user's PDF earlier in this conversation — the raw table text is already in context and does not require re-reading the PDF during implementation.

Confirmed product decisions (from conversation):
- Party movement stays overland-only (unchanged `MOVE_PARTY_TO` adjacency rule).
- Clicking any **revealed** hex now navigates to a dedicated full-view screen (replacing the current `SELECT_HEX` → sidebar behavior). Clicking an unrevealed-but-revealable hex is unchanged.
- The dice-drop-on-paper mechanic becomes a **blob/footprint grid layout**: rooms/districts placed via random-walk clustering on an abstract square grid, rendered as adjacent filled cells (the cell cluster itself *is* the visual shape — no separate outline/hull math).
- MongoDB backend supports full multi-campaign save/load/list/delete, with generated site/settlement content nested inside each campaign's map document.
- Random encounter rolling is a standalone action on any revealed hex (terrain-based) and on any settlement district (district-based).

Two mechanical points from the book that correct assumptions floated during planning — verified directly against the transcribed pages, not left as open questions:
1. **Site Type and Settlement Type are independently, freshly rolled at generation time — never derived from the overland POI's `location` text.** Page 130 ("Shadowdark Maps") and page 134 ("Settlement Maps") both present Site Size/Type and Settlement Type as standalone d6 rolls with zero cross-reference to the overland Points of Interest table. The POI's `location`/`development` text is shown as flavor context only. This eliminates any need for a `POI_LOCATION_TO_SITE_TYPE` mapping table.
2. **Room/district count is the literal number of dice, never a sum.** "Small = 5d10" means 5 rooms (one die per room); "Village = 3d4" means 3 districts (one die per district) — not "roll 5d10 and sum them." Each individual die's face value is *also* that room/district's type-table index directly (same physical die serves both placement and type, per the book's own wording: "note the number on each die to determine the type of room/district it is"). This matters for settlements specifically: Village/Town use d4 dice (so district type can only ever land in indices 1-4: Slums/Low/Artisan/Market), City uses d6 (indices 1-6, reaching up to Temple District but never University or Castle), and only Metropolis uses d8 (full range, including Castle). This tiering must be preserved exactly — it's intentional RAW, not a bug to "fix" later.

## Data layer

New files alongside `src/data/tables.ts` (same one-file-per-book-section precedent, plain `Record`/array constants + pure lookup functions, no classes, no side effects):

- **`src/data/dungeonTables.ts`** — Site Size d6 (`{size, roomCount}` for Small=5/Medium=8/Large=12), Site Type d6 (Cave/Tomb/Deep tunnels/Ruins — fresh roll, see correction above, no POI mapping), Room Type d10 (10 entries) + per-type detail sub-tables (Trap/Minor Hazard/Major Hazard/Solo Monster/Monster Mob/NPC/Treasure/Boss Monster — some are two-column "roll twice" tables per the book, e.g. Trap has a Detail+Detail d6 pair), dungeon Danger Level d6.
- **`src/data/settlementTables.ts`** — Settlement Type d6 → `{type, diceCount, diceSides}` (Village 3/d4, Town 4/d4, City 6/d6, Metropolis 8/d8 — one dice spec per tier, no separate "count" vs "type die" fields), District Type lookup (indexed 1..diceSides against the 8-entry Districts table), per-district POI tables (×8, one per district type), Alignment d6, Taverns (size tiers, name generator, food/drink tables), Shops (poor/standard/wealthy + generator + interesting customer).
- **`src/data/encounterTables.ts`** — 20 d100 tables (~100 entries each) as a single `Record<EncounterTableKey, string[]>` plus `TERRAIN_TO_ENCOUNTER_KEYS` (handles the two-terrain-per-key cases: Desert/arctic, Forest/jungle) and `DISTRICT_TYPE_TO_ENCOUNTER_KEY` (clean 1:1). Kept as one file since it's flat data with a single lookup function — split later only if review finds it unwieldy.

Sub-table dispatch follows the existing `rollPointOfInterest` precedent exactly: table files expose pure `roll -> result` lookups only; the **engine** (not the table file) decides when to roll an inner die and which table to consult next.

## Generation engine

New files under `src/engine/`, each pure and `Rng`-injectable like `generateHex.ts`:

- **`src/engine/gridLayout.ts`** — `generateGridLayout(count, rng, options?)`: place item 0 at `{row:0,col:0}`; for each subsequent item, pick a random already-placed anchor, place into one of its free orthogonal neighbors (random choice among available); if none free, retry a different anchor (bounded attempts), falling back to nearest-free-cell BFS to guarantee termination/connectivity. Returns `GridPlacement[] = {cell, parentIndex}[]`. Orthogonal-only by default (clean rectilinear look); this one shared function drives both dungeon rooms and settlement districts.
- **`src/engine/generateDungeon.ts`** — `generateDungeonSite(poi, rng, overrideSiteType?)`: roll size (d6) → roll type (d6, or use override) → roll danger (d6) → `generateGridLayout(roomCount, rng)` → per room roll Room Type (d10) + contingent detail → mark the single highest-rolling room `isObjectiveRoom` (first occurrence wins ties).
- **`src/engine/generateSettlement.ts`** — `generateSettlement(poi, rng, overrideSettlementType?)`: roll settlement type (d6, or override) → look up its dice spec → `generateGridLayout(diceCount, rng)` → per district roll type (1..diceSides, direct index — no separate count/type rolls) + alignment (d6) + POI count (1d4) + that many POI rolls against the district's own table → mark highest-rolling district `isSeatOfGovernment`.
- **`src/engine/generateTavern.ts`, `generateShop.ts`** — ephemeral, re-rollable generators (ATT: not persisted onto a District — only site/settlement generation itself needs to be idempotent).
- **`src/engine/rollEncounter.ts`** — `rollEncounter(key, rng)`.
- **`src/engine/generateSite.ts`** — dispatcher: `generateSiteForHex(poi, rng)` checks `SETTLEMENT_LOCATIONS.includes(poi.location)` (reusing the existing constant from `tables.ts`) to route to `generateSettlement` vs `generateDungeonSite`.

## State management

`Hex` gains an optional `site?: GeneratedSite` (discriminated union `DungeonSite | Settlement`, tagged by `kind`). New `MapAction` variants:

```ts
| { type: 'GENERATE_SITE'; hexId: string; rng?: Rng }   // idempotent: no-op if hex.site already set
| { type: 'REROLL_SITE'; hexId: string; rng?: Rng }       // unconditional regenerate
```

`GENERATE_SITE` auto-dispatches on first mount of a POI hex's full view; `REROLL_SITE` is an explicit button, mirroring the existing `REROLL_HEX` convention.

**Reducer fix required on existing actions** (staleness guard): `REROLL_HEX` and `EDIT_HEX` (when the patch touches `poi`) must clear `site: undefined`, since a generated site is derived from a specific `poi` — needs new regression tests in `mapReducer.test.ts`.

Campaign identity (id/name/timestamps) is added as a **wrapper type**, not new `MapState` fields — keeps the reducer purely about game rules:

```ts
// src/state/campaign.ts (new)
export type CampaignMeta = { id: string; name: string; createdAt: string; updatedAt: string }
export type Campaign = CampaignMeta & { map: MapState }
```

## UI / navigation

Add `react-router-dom`. Justified because campaign ids now exist regardless (needed for Mongo) — encoding them in the URL is nearly free and buys real value (deep links to a specific hex's dungeon, correct back/forward, refresh-safe). Routes:

```
/                                     CampaignListPage   (landing: list/open/new/delete campaigns)
/campaigns/new                        CampaignMapPage     (today's StartMapDialog flow, unsaved until Save)
/campaigns/:campaignId                 CampaignMapPage     (loads from API, renders overland map)
/campaigns/:campaignId/hex/:hexId      HexDetailPage        (full view)
```

**`HexDetailsPanel.tsx` is retired.** Its Move/Reroll/Edit responsibilities move into a shared `HexBaseInfo` component used by all three full-view variants — there is no longer a persistent sidebar on the map screen, since clicking a hex now navigates away instead of just selecting it. This directly follows from "full view would be better" than an expanded panel.

New components:
- `src/routes/CampaignListPage.tsx`, `CampaignMapPage.tsx`, `HexDetailPage.tsx`
- `src/components/hexdetail/HexBaseInfo.tsx` (id/terrain/danger, move/reroll/edit, `EncounterRoller`), `WildernessView.tsx` (no-POI case), `DungeonSiteView.tsx`, `SettlementView.tsx`
- `src/components/EncounterRoller.tsx` — local-only, no dispatch, dropdown when a terrain maps to 2 tables (Desert/arctic, Forest/jungle)
- `src/hexgrid/GridLayoutSvg.tsx` — parallel to `HexGridSvg.tsx`: renders `{cell, colorKey, label, parentId}[]` as adjacent `<rect>`s (cell adjacency *is* the shape) plus corridor `<line>`s between parent/child; `data-room-id`/`data-district-id` attributes for Playwright targeting, matching the existing `data-hex-id` convention.

**Click behavior change** in `HexGridSvg.tsx` (confirmed low-risk — read directly, lines 116-122): the `if (hex) dispatch({type:'SELECT_HEX', ...})` branch becomes `if (hex) navigate(...)`; the `MOVE_PARTY_TO` branch for revealable-but-unrevealed hexes is untouched.

## Backend for MongoDB

```
server/
  src/index.ts            Express bootstrap (cors, json body-parser), mounts /api/campaigns
  src/db.ts                 MongoClient singleton
  src/routes/campaigns.ts    5 REST endpoints below
  src/types.ts               type-only relative imports of MapState/Hex/GeneratedSite from ../../src/state/*
  package.json                own deps: express, mongodb, cors, dotenv; dev: tsx, typescript
  .env.example / .env (gitignored)   MONGODB_URI, MONGODB_DB, PORT
```

Separate `server/package.json` (not folded into root) keeps `express`/`mongodb` out of the Vite-bundled client's dependency graph. Use the official `mongodb` driver, not Mongoose — no cross-document references exist (sites are embedded, not linked), so Mongoose's schema/population features add nothing here.

**Reuse note:** the actual MongoDB instance to point at is a shared Atlas cluster the user already uses across their RPG-tooling projects (`cluster0.gkij9qt.mongodb.net`, credential currently in `~/.bashrc` as `MONGO_URI`, pointed at a `BasicFantasy` database for an unrelated project) — this project should use the **same cluster, a new/separate database** (e.g. `ShadowdarkHexCrawl`), never `BasicFantasy`.

**Future scope flagged during the monster/NPC population phase (not designed yet, don't build prematurely):** the user confirmed real monster stat blocks (AC/HP/attacks/etc., beyond the name-only `src/data/monsterTables.ts` data that shipped in the "Dungeon monster/NPC population" phase above) are planned as a *later* MongoDB-backed lookup, sequenced after this Mongo backend phase. When that work starts: design a `monsters` collection (schema TBD — possibly informed by `shadowdark-rest`'s own `Monster.scala`/`MonsterRepository`, though that project's Mongo data is a different, unrelated collection/database, not something to point at directly), and change `Room.monster` from a bare `{name, category}` to something that can resolve to a full stat block (e.g. a lookup key). No changes needed for this now — noting it here so the Mongo backend phase's schema doesn't need to be redesigned twice.

Routes:
```
GET    /api/campaigns          -> CampaignSummary[] (id, name, timestamps, hexCount — no nested hexes; no `radius`, dropped in the arbitrary-size-maps phase)
GET    /api/campaigns/:id       -> Campaign (full nested map) | 404
POST   /api/campaigns           body:{name, map}       -> 201 Campaign  (save-as-new)
PUT    /api/campaigns/:id       body:{name?, map}       -> 200 Campaign  (save existing, bumps updatedAt)
DELETE /api/campaigns/:id        -> 204 | 404
```

Dev tooling: root adds `concurrently`; `npm run dev` runs `vite` + `server`'s `tsx watch` together; `vite.config.ts` gets `server.proxy: {'/api': 'http://localhost:4000'}` so the frontend can `fetch('/api/...')` with no CORS config in dev.

Frontend: new `src/persistence/api.ts` (`listCampaigns`, `loadCampaign`, `createCampaign`, `updateCampaign`, `deleteCampaign`). `src/persistence/localStorage.ts` is kept as a fast local draft cache (crash recovery for "current campaign in progress"), no longer the source of truth. `MapProvider` takes an optional `initialState` prop so `CampaignMapPage` can inject an API-loaded campaign; `/campaigns/new` passes none (today's exact flow, unsaved until the user hits Save). UI: explicit "Save" button in `Toolbar` (dirty indicator, last-saved time, visible error on failure) **plus** a debounced (~30s) autosave safety net — network writes need visible feedback that silent-only debounce can't give, but an explicit-only save risks losing a forgetful GM's session.

## Testing

New Vitest files mirroring existing conventions (scripted/seeded `Rng`, never real `Math.random()`):
- `gridLayout.test.ts` — no-overlap, full-connectivity (every node's parent chain reaches index 0), correct count, across sizes 0-64.
- `dungeonTables.test.ts`, `settlementTables.test.ts` — `it.each` over full table ranges; **explicit regression test that Village/Town (d4 dice) can never produce a district-type index above 4** — the single most important invariant in this feature, since it's easy to "fix" by accident later.
- `encounterTables.test.ts` — structural (each of 20 keys has exactly 100 entries, out-of-range throws) rather than content-exhaustive.
- `generateDungeon.test.ts`, `generateSettlement.test.ts` — exact scripted-roll assertions including objective-room / seat-of-government tie-break behavior.
- `mapReducer.test.ts` (extended) — `GENERATE_SITE`/`REROLL_SITE` behavior + the new staleness-clearing regression tests on `REROLL_HEX`/`EDIT_HEX`.

Real-browser Playwright verification required (per this project's documented history of 3 bugs that passed typecheck/build but broke visually — pointer capture, passive-wheel preventDefault, indefinite flex height): new `GridLayoutSvg` rendering/sizing, router back/forward + hard-refresh-on-deep-link behavior, and async campaign-load UX (no flash-of-empty-state under StrictMode's double-invoked effects).

## Phasing

1. ✅ **Data + engine + unit tests, no UI.** All new `data/`/`engine/` files above, full Vitest coverage. Zero risk to the currently-working app; `npm run test` / `npx tsc -b` green is the stopping point. — Done.
2. ✅ **Hex full-view UI + navigation, local generation only, no Mongo.** Router, new routes/components, reducer changes, retire `HexDetailsPanel`. Playwright pass. Stopping point: fully playable single-campaign app, generation works, persistence still localStorage-only. — Done.
3. ✅ **Random encounters wiring** — mostly delivered by phase 2's `EncounterRoller`; this phase adds the settlement-district-level rolling + Tavern/Shop generator buttons. — Substantially done as part of phase 2.
4. ✅ **Arbitrary-size hex maps** — inserted here, before Mongo, specifically so the persisted campaign schema never has to carry (and later migrate away) a `radius` field. See "Arbitrary-size hex maps" section below for the design. — Done: `radius` removed from `MapState`/`START_MAP`/`StartMapDialog`; `hexMath.ts`'s `computeVisibleCoords` replaces `hexesInRadius`/`isWithinRadius` (both deleted as dead code); `isRevealableNow`/`MOVE_PARTY_TO` now only check party-adjacency. Browser-verified via Playwright (walked 4 hexes out with no refusal, confirmed frontier de-duplication, no `radius` key persisted, zero console errors) plus unit tests in `hexMath.test.ts`/`mapReducer.test.ts`.
5. ✅ **Real dungeon maps** — BSP-style room layout (variable-sized rooms tiling a coherent floor plan) replacing the uniform grid-blob dungeon cells. No new dependency. See "Real dungeon maps" section below for what shipped vs. what was deliberately deferred (corner-notch outline, hallway carving, entrance markers) — Done.
6. ✅ **Dungeon monster/NPC population** — real monster/NPC names (not book stat blocks — see below) wired into the existing "Solo Monster"/"Monster Mob"/"Boss Monster"/"NPC" room-content rolls. See "Dungeon monster/NPC population" section below for what shipped and the deliberate name-only scope — Done.
7. ✅ **Real settlement maps** — Voronoi districts clipped to an organic jittered town boundary, curved road network, replacing the uniform grid-blob settlement districts. New geometry-library dependencies (`d3-delaunay` + `polygon-clipping`) — first such dependencies in this project. **Revised same-day** after the first version was rejected as looking like an abstract diagram rather than a map — added building/park footprints, visible streets, and a towered wall, targeting a reference image the user provided. See "Real settlement maps" section below (including the "Revision" subsection) for what shipped — Done.
8. ✅ **Location Generator expansion** — inserted here, before Settlement NPC population and Mongo, 2026-07-04: a house-rule d200 overland POI table plus 5 new site kinds (Tower, Shrine, Rift, Keep, Camp) so dungeon-site variety matches the overland POI table's 20+ flavors. Sequenced before Mongo for the same schema-stability reason as every other phase here — `Hex.site`'s `GeneratedSite` union is still changing shape. See "Location Generator expansion" section below. **Done, browser-verified 2026-07-05** — all 5 new site kinds plus the batched POI-table/dispatch cutover (full d200 table, all 10 site kinds routed, settlement-type forcing from POI labels) are complete.
9. **Settlement NPC population** — reuses phase 6's Monster/NPC engine to populate district POIs with named NPCs. See "Settlement NPC population" section below. Not started, design not yet fully specified.
10. **Mongo backend + multi-campaign persistence** (renumbered from the original phase 4, then again after phases 5-9 were inserted) — the most novel/riskiest territory, independently verifiable once phases 1-9 already work. `server/` scaffold, routes, dev tooling, `api.ts`, `CampaignListPage`, async `MapProvider`, Save button + autosave, one-time "import current local map as a new campaign" action for the existing single localStorage slot. Not started.
11. **Neighbor-weighted terrain generation (incl. making Ocean generation coherent)** — sequenced last since it's a pure `engine/generateHex.ts` change with zero schema/storage implications either way; no reason to block anything else on it. See "Neighbor-weighted terrain generation" section below. Not started, design not yet fully specified.

## Arbitrary-size hex maps

**Why now, before Mongo:** the current model picks a fixed `radius` at `START_MAP` time and `hexesInRadius(radius)` (in `hexMath.ts`) enumerates the *entire* disk of that radius on every render — including every unrevealed cell — so `HexGridSvg` currently draws thousands of fog cells for a large radius. That can't extend to a truly unbounded map (you can't enumerate infinite hexes), and it's wasteful even at a large-but-still-fixed radius. Doing this before Mongo avoids ever persisting a `radius` field into the campaign schema and then having to migrate it away later.

**Design:** switch from "render every hex within a fixed radius disk" to "render only revealed hexes plus their frontier" — the standard fog-of-war approach for unbounded procedural maps, which scales with the explored area instead of a hypothetical max size.

- `frontierCoords = union(neighborsOf(hex)) for every revealed hex, minus already-revealed coords`. `visibleCoords = revealedCoords ∪ frontierCoords`.
- `src/hexgrid/HexGridSvg.tsx`: replace `hexesInRadius(state.radius)` with `visibleCoords` computed from `state.hexes`. Revealed hexes render as today; frontier hexes adjacent to the *party* keep today's clickable "revealable" style; other frontier hexes (adjacent to some revealed hex, but not the party) get the plain "fog" style — same two visual categories as today, just derived from the explored region's actual shape instead of a bounding disk.
- `computeFitViewBox` (same file): drive the bounding-box fit from `visibleCoords` instead of `hexesInRadius(radius)`.
- `src/state/mapReducer.ts`: drop `radius` from `MapState` and from the `START_MAP` action payload entirely. `isRevealableNow` and the `MOVE_PARTY_TO` case drop their `isWithinRadius` check — the only remaining bound is party-adjacency + not-already-revealed.
- `src/hexgrid/hexMath.ts`: `isWithinRadius`/`hexesInRadius` likely become dead code once the above lands — remove them if nothing else references them (check tests first).
- `src/components/StartMapDialog.tsx`: remove the "Map radius" number input — a new map just starts at hex `(0,0)` with a chosen/rolled terrain, no size to pick up front.
- Tests: `mapReducer.test.ts`'s "MOVE_PARTY_TO refuses a hex beyond the map radius" and the radius-based case in "isRevealableNow" need reworking — a hex should now only ever be refused for non-adjacency, never for radius. Add a test confirming a hex arbitrarily far from the origin is reachable by walking there one adjacent step at a time.
- Playwright verification required per this project's convention — this touches SVG rendering/viewBox fitting, exactly the class of change that broke silently before (see CLAUDE.md's three documented bugs).

## Real dungeon maps

**Confirmed requirement, 2026-07-04 — done.** Sequenced before Mongo (like arbitrary-size hex maps) so the persisted dungeon schema is settled before it's ever written to MongoDB.

**Why:** `generateDungeonSite` (`src/engine/generateDungeon.ts`) used to place rooms via `gridLayout.ts`'s random-walk clustering onto uniform grid cells — deliberately simple per this project's original design, but not a "real" map: every room was the same size, laid out as a tree of unit cells. `gridLayout.ts` itself is untouched and still used by settlements (phase 7 will address that).

**What shipped — a BSP-style floor-plan layout, algorithm ported from the sibling project `~/dev/source/shadowdark-rest`'s `DungeonServer.scala` (not literal code; different stack), deliberately scoped down from its full feature set:**
- **New `src/engine/dungeonLayout.ts`** — `generateDungeonLayout(roomCount, rng)`: starts from a single bounding rectangle sized to the room count (`cols = ceil(sqrt(roomCount))`, `rows = ceil(roomCount / cols)`, `UNIT = 10` abstract units/room), then repeatedly splits the largest-area splittable rectangle (BSP) along its longer axis at a randomized position, until there are exactly `roomCount` leaf rectangles. `MIN_ROOM_DIM = 4` is enforced by construction (a split only ever shrinks the split axis down to the minimum, never touches the other axis, so by induction every leaf stays above the minimum on both axes — no post-hoc validation needed). Consumes exactly `roomCount - 1` `rng()` calls (one per split).
- **Connectivity via real geometric adjacency, not a parent tree**: after the splits, every pair of leaf rectangles that share a boundary segment of positive length (not just a corner touch) gets a connection — a room can now connect to more than one neighbor, unlike the old single-`parentIndex` model. A guillotine-cut rectangular subdivision's adjacency graph built this way is always fully connected; verified empirically across sizes 5/8/12 and many seeds in `dungeonLayout.test.ts` (BFS reachability from room 0) rather than proven algebraically.
- **`generateDungeon.ts`'s `Room` type** changed from `{ cell: GridCell; parentRoomId: string | null }` to `{ rect: Rect }`; `DungeonSite` gained a top-level `connections: [string, string][]` (room-id pairs) replacing the per-room parent pointer.
- **New `src/hexgrid/DungeonMapSvg.tsx`** (parallel to `GridLayoutSvg.tsx`, which settlements still use) renders the real pixel-space rectangles plus corridor lines between connected room centers. `data-room-id` Playwright targeting convention preserved.
- **Deliberately deferred as later-if-wanted polish, not required for the "real rooms" upgrade** (scope call made during implementation, not asked as a separate question): corner-notch non-rectangular outline, hallway-strip carving (slicing a leaf into `room/hallway/room`), and entrance markers. The core value — variable-sized real rooms tiling a coherent floor plan, replacing uniform grid-blob cells — does not depend on any of these; each is a self-contained additive enhancement if wanted later.
- No new geometry-library dependency needed — pure rectangle-splitting math, no polygon boolean ops.

**Verification:** 9 new tests in `dungeonLayout.test.ts` (no-overlap, minimum size, full connectivity across sizes 5/8/12 and 4 seeds each, valid connection indices) + updated `generateDungeon.test.ts` scripted-rng tests (BSP consumes `roomCount - 1` rng calls vs. the old grid layout's `2 × (roomCount - 1)`). `npx tsc -b` and `npm run build` clean, all 169 Vitest tests pass. Browser-verified via Playwright: generated an 8-room dungeon and confirmed 8 distinct rendered rectangle sizes (proving real variable-size rooms, not uniform cells) and 14 corridor lines (proving multi-neighbor adjacency, not a 7-edge single-parent tree); Reroll Site regenerates correctly; zero console errors.

**Known gap, confirmed 2026-07-04 after the user reviewed a live screenshot:** what shipped is a *structural* upgrade (real room shapes/sizes, real adjacency-based connections) rendered as flat colored rectangles — not yet a *visual* one. `DungeonMapSvg.tsx` draws corridor `<line>`s under the rooms, so they're barely visible between directly-adjacent (touching) rooms; there's no wall/door distinction, no distinct corridor rendering, no grid overlay, no entrance markers. User confirmed: **defer visual polish, don't block Phase 6 on it** — fold it into a later pass, likely bundled with "Real settlement maps" (phase 7 below) so both dungeon and settlement rendering get the same visual-fidelity treatment together rather than doing it twice. No schema change needed when that happens — `Room.rect`/`DungeonSite.connections` already carry everything a richer renderer would need.

**Follow-up (same day, phase 7's revision):** the settlement side hit the identical z-order/flat-color visibility problem independently, and got fixed as part of a full settlement-rendering rebuild (see "Real settlement maps" → "Revision: v1 looked like a diagram, not a map" below) — but dungeons were **not** revisited as part of that fix; the "bundle it together" plan sketched above didn't end up happening in practice, since the settlement fix grew into its own dedicated pass. Dungeon visual polish was picked back up separately shortly after — see the revision subsection below.

### Revision: organic cave rendering + rectangular polish (same day, 2026-07-04)

**What prompted it:** after the settlement map got its illustrated-style upgrade, the user asked for dungeons ("tunnels") to get a matching visual upgrade, pointing to [a hand-drawn cave-map reference](https://img.itch.zone/aW1hZ2UvMTYyNjU3Ny8xMzk2MzAzNi5wbmc=/original/7pXk3g.png) — organic irregular cavern rooms, thick crosshatched rocky walls, corridors that visibly narrow between rooms, and a background grid. Fetched and viewed the image directly before rebuilding.

**Scope decision confirmed before writing code:** the existing BSP layout produces rectangular rooms for *all four* site types (Cave, Tomb, Deep tunnels, Ruins), but a natural cave and a built tomb/ruin shouldn't plausibly look the same. Asked and confirmed: Cave and Deep tunnels get the new organic-cavern rendering; Tomb and Ruins keep rectangular rooms, with a smaller matching visual-polish pass (thicker walls, a brick-textured wall band) rather than a full redesign — preserves the thematic distinction the book's site types already carry.

**Key technical decision — this is purely a rendering-layer feature, not a generation-engine change.** `dungeonLayout.ts`/`generateDungeonSite`'s rects and connections are completely unchanged; a room's organic blob shape is derived deterministically from its existing `rect` via a cheap seeded hash (`seedForRect`), so the same room always renders the same cavern silhouette without new generated/persisted data or touching `generateDungeonSite`'s rng stream. New `src/engine/caveRenderShapes.ts` (pure, unit-tested, not `Rng`-injectable in the generation sense — deterministic from a numeric seed instead):
- `generateBlobShape(seed)` — N angles with jittered radius factors (a reusable "wobble").
- `blobToPolygon(center, baseRadius, shape, extraRadius)` — renders that wobble to a closed polygon at a given radius; calling it twice with `extraRadius = 0` (floor) and `extraRadius = wallThickness` (wall band) gives two polygons that follow the same organic contour at a roughly constant offset — this is how the crosshatched wall band gets its shape.
- `corridorPolygon(a, b, halfWidth)` — a straight quadrilateral ribbon between two points, used for corridors in both styles (not a true rounded capsule — reads fine at this render scale, kept simple).

**Real problem found and fixed along the way, affecting both styles**: rectangular rooms tile edge-to-edge with zero gap (by construction — that's what makes the BSP floor plan coherent), so a corridor line between two touching rooms had literally no visible gap to occupy regardless of its own styling — the root cause of the original "barely visible corridors" complaint from phase 5, never actually fixed until now. Fix: rectangular rooms are now inset from their true `rect` bounds (`RECT_ROOM_INSET_UNITS`), leaving a deliberate gap for the corridor band to occupy. Cave-style blobs didn't need this fix — a blob's radius is already smaller than its room's full rect, so a natural gap exists at the rect boundary without any extra work.

**What shipped in `DungeonMapSvg.tsx`** (fully rewritten, `caveStyle` boolean prop set by `DungeonSiteView.tsx` based on `site.siteType`):
- Three SVG `<pattern>` defs, no raster assets: `dungeon-grid` (a VTT-style light square grid, used as the whole canvas background in both styles), `dungeon-crosshatch` (two overlaid diagonal line sets — an actual crosshatch, not raster texture, for Cave/Deep tunnels), `dungeon-brick` (a running-bond brick grid for Tomb/Ruins).
- Both room styles render as two layers: an outer "wall band" shape filled with the site-type-appropriate pattern, and an inner "floor" shape filled with the existing `ROOM_TYPE_COLORS` color (preserving the at-a-glance room-type color coding from before) — blob polygons for Cave/Deep tunnels, rectangles for Tomb/Ruins.
- Corridors render the same two-layer treatment (wall-band + floor quadrilateral) for both styles now, not a bare line — drawn before rooms so rooms visually cover the parts that overlap them, leaving only the true connecting span visible.
- Labels gained a dark stroke halo (same technique as the settlement renderer's labels) for readability against the new busier/textured background.

**Verification**: 8 new tests in `caveRenderShapes.test.ts` (seed determinism, blob shape validity, polygon closure, inflation-increases-distance-from-center, corridor quadrilateral geometry including a degenerate coincident-points case). All 190 Vitest tests pass, `npx tsc -b`/`npm run build` clean (bundle ~337KB, ~112KB gzipped). Browser + screenshot verified via Playwright for both styles: a "Deep tunnels" site rendered 5 organic blob rooms with visible crosshatched walls and corridors between them; a "Ruins" site rendered rectangular rooms with a visible brick-patterned wall band and — confirming the corridor-visibility fix actually worked — clearly visible corridor bands between rooms where there used to be none. Room click-to-expand still works in both styles. Zero console errors.

**Follow-up fix, same day: corridors were visible but still too straight.** The user reviewed a live 12-room "Deep tunnels" screenshot and flagged that every corridor was a perfectly straight quadrilateral — with several converging on one room, the layout read as a network/graph diagram rather than a winding cave system, undercutting the whole point of the organic-cave rendering. Fixed for cave style only (Tomb/Ruins correctly keep straight halls — a built structure plausibly has them): `caveRenderShapes.ts` gained `generateOrganicCorridorWaypoints(a, b, seed)` (2 intermediate waypoints, each offset perpendicular to the straight line by a deterministic jittered amount) and `smoothPathData(points)` (the standard "smooth freehand curve through N points" construction — each interior point is a quadratic-bezier control point curving toward the midpoint of itself and the next point, so the path flows through bends instead of having sharp polyline corners). `seedForConnection(seedA, seedB)` XORs two rooms' existing `seedForRect` seeds (order-independent) so the same pair of rooms always winds the same way. `DungeonMapSvg.tsx`'s cave-style corridors now render as two stacked SVG `<path>` strokes along this winding centerline (thick hatch-patterned "wall" stroke + thinner solid "floor" stroke, both round-capped/joined) instead of a straight polygon ribbon — native SVG stroking handles the bends correctly with no extra ribbon-polygon geometry needed. 10 new tests (waypoint determinism, endpoints preserved exactly, at least one waypoint deviates from the straight line, different seeds diverge, path-string construction). All 200 Vitest tests pass, build clean. Browser + screenshot verified: a 12-room Deep tunnels dungeon now shows visibly winding, curved corridors between rooms rather than straight lines, zero console errors.

## Dungeon monster/NPC population

**Confirmed requirement, 2026-07-04 — done.** Sequenced immediately after "Real dungeon maps," before Mongo.

**Why:** dungeon rooms already roll "Solo Monster"/"Monster Mob"/"Boss Monster"/"NPC" as Room Type content (`src/data/dungeonTables.ts`), but that only produced book-table flavor text (e.g. "Mighty Brute"), not an actual named entity. Confirmed scope: dungeons + settlements only (not overland random encounters).

**Scope clarified during implementation, before writing any code:** the user was asked (1) how much monster detail to attach and (2) where the data should come from. Answer to both: **name + flavor only for now, no combat stats** — real stat blocks are an explicitly deferred **future MongoDB integration** (sequenced after the Mongo backend phase, not designed yet). This ruled out both transcribing a full Shadowdark bestiary (large, and not actually wanted yet) and inventing stat numbers (would misrepresent RAW). The user also rejected a first attempt at a small invented/fabricated monster-name list — the actual source used is real, book-adjacent content the user already maintains: `~/dev/source/b_x/publication/monsters/combined-monsters.md`, a "B/X Book of Monsters" the user personally compiled (merged/deduplicated from the 1981 D&D Basic and Expert Sets). This is a different game system's bestiary than Shadowdark's own (no Shadowdark bestiary was available), used here purely as flavor-name content, not claimed as Shadowdark RAW.

**What shipped:**
- **New `src/data/monsterTables.ts`** — `MONSTERS: MonsterEntry[]` (148 real creature names + their source-document category — Animal/Undead/Humanoid/Dragon/Monstrous/etc. — transcribed from the B/X compilation above) and `NPC_TYPES: string[]` (17 NPC archetypes — Acolyte, Bandit, Noble, Trader, Veteran, etc. — from that same document's own "NPCs" category; its "Men" umbrella entry was expanded into its 5 named sub-types — Brigand, Buccaneer/Pirate, Dervish, Merchant, Nomad — and "NPC Parties," a party-generation ruleset rather than a single NPC, was excluded).
- **`generateDungeon.ts`**: `Room` gained `monster?: GeneratedMonster` (set for Solo Monster/Monster Mob/Boss Monster room types) and `npc?: GeneratedNpc` (set for NPC room type), rolled immediately after the existing detail roll for that room.
- **`DungeonSiteView.tsx`**: room list now shows `Monster: <name> (<category>)` or `NPC: <type>` alongside the existing descriptor text.
- No schema design was needed for the future Mongo stat-block integration yet — deferred until that phase is actually scoped.

**Follow-up fix, same day (2026-07-04), after the user reviewed a live generated dungeon and flagged it as having "no rhyme or reason"** — concretely: a Boss Monster room attached "Cattle" (a farm animal, absurd as a dungeon's climactic threat), and monster category had zero relationship to the dungeon's site type (Cave/Tomb/Deep tunnels/Ruins pulled from the same flat pool). User approved fixing both now (a third option, biasing detail-text rerolls to avoid exact duplicates like two "Collapsing walls" rooms, was **not** selected — deferred):
- **`MonsterEntry` gained `mundane?: boolean`** — hand-curated (not derived from any transcribed data) on ~25 ordinary real-world Animal-category entries (Cattle, Horse, Bull, Wolf, Rat, etc.); "Giant"/"Great"-qualified variants (Giant Beaver, Cat/Great, etc.) are legitimate escalated B/X monsters and are **not** marked mundane. `rollMonster(rng, { excludeMundane: true })` filters them out — wired to Boss Monster only; Solo Monster/Monster Mob still draw from the full pool (a wolf pack or giant rats are normal low-stakes encounters, just not a dungeon's final boss).
- **`SITE_TYPE_CATEGORY_WEIGHTS`** (also in `monsterTables.ts`) biases category selection per `SiteType` — Cave leans Animal/Lost World/Insect/Monstrous/Lycanthrope; Tomb leans heavily Undead (+Construct); Deep tunnels leans Insect/Monstrous/Humanoid/Giant/Elemental; Ruins leans Construct/Humanoid/Undead/Sylvan-or-Faerie. Categories not listed for a site type keep a baseline weight (not zero) rather than being fully excluded, so an occasional off-theme surprise stays possible. `rollMonster(rng, { siteType })` now picks a category first (weighted), then an entry uniformly within it — this is a bias, not a hard filter. Wired to all three monster room types (Solo Monster/Monster Mob/Boss Monster) via `generateDungeon.ts` passing the dungeon's own rolled `siteType`.
- Curation is a subjective judgment call (mundane list and category weights alike), not derived from any source data — reasonable to revisit later if it feels off in play.
- **Verification**: `monsterTables.test.ts` grew from 5 to 9 tests (every category retains at least one non-mundane entry; `excludeMundane` never returns a mundane entry; a 500-sample statistical check confirms Tomb's Undead weighting actually dominates the distribution; `siteType` + `excludeMundane` compose correctly together). `generateDungeon.test.ts`'s scripted-rng test updated (monster rolls now consume 2 `rng()` calls — category pick + entry pick — instead of 1) and its exact-value assertion now confirms a real non-mundane result (`{name: 'Beaver, Giant', category: 'Animal'}`) rather than just `toBeDefined()`. All 178 Vitest tests pass, `npx tsc -b`/`npm run build` clean. Browser-verified via Playwright across ~5 generated dungeons: 0 mundane violations across Boss Monster rooms encountered, Cave dungeons showed the expected Animal/Lost World lean, zero console errors (the statistical rigor of the weighting itself lives in the unit test's 500-sample check — this pass confirmed no integration bugs in the actual wired-up app).

**Verification:** new `monsterTables.test.ts` (no duplicate names, every roll returns a table entry) + extended `generateDungeon.test.ts` invariants (every Solo Monster/Monster Mob/Boss Monster room has a `monster`, every NPC room has an `npc`, no other room type has either) plus updated scripted-rng tests (monster rolls consume one extra `rng()` call per applicable room). All 174 Vitest tests pass, `npx tsc -b`/`npm run build` clean. Browser-verified via Playwright: generated a 12-room dungeon with 3 monster rooms (Spectre, Wight, Wraith — all Undead category, coincidentally, since this was one random roll) and 1 NPC room (Noble), confirmed both "Monster:" and "NPC:" text render in the DOM, zero console errors.

## Real settlement maps

**Confirmed requirement, 2026-07-04 — done.** The biggest lift of the four new phases; this project's first geometry-library dependency. Sequenced before Mongo.

**Why:** `generateSettlement` (`src/engine/generateSettlement.ts`) used to lay out districts the same grid-blob way as dungeon rooms. The user confirmed this needed to become a real, visually organic settlement map, matching the fidelity already built in `shadowdark-rest`'s `SettlementServer.scala` (explicitly chose full Voronoi/organic fidelity over a lighter rectangle-only alternative).

**Scope simplification made before writing code, to cut the two heaviest geometry operations down to one:** rather than (a) generating districts as unbounded Voronoi cells and separately (b) unioning them to derive an outline which then gets buffered/simplified/roughened for an organic look (shadowdark-rest's approach), this project generates the organic jittered boundary ("city mask") **first**, then clips every Voronoi cell to that mask directly. The mask itself already IS the organic outline — no separate union-then-buffer pass is needed. This also meant `turf.js` (which shadowdark-rest's JTS-based union/buffer would map to) wasn't needed at all; only a Voronoi library and a polygon-intersection (clip) library were.

**Dependencies added**: `d3-delaunay` (Voronoi/Delaunay computation, ~104KB unpacked, one small dependency `delaunator`) and `polygon-clipping` (general polygon boolean ops for the mask-clip step, ~350KB unpacked, two small dependencies). Both ESM-native, verified compatible with this project's Vite 5/Node 20 toolchain via a direct smoke test before adopting. `npm audit` after install showed only the same pre-existing esbuild/vite dev-server-only advisory already documented in this project's "Stack & environment gotchas" — neither new package introduced a new advisory.

**Real bug found and worked around**: `polygon-clipping@0.15.7`'s published `.d.ts` declares named exports (`export function intersection(...)`), but its actual ESM bundle only exports a single `default` object containing those functions — confirmed by direct testing (`import { intersection } from 'polygon-clipping'` typechecks but is `undefined` at runtime under Vite's ESM-native loading). Isolated the workaround in a new `src/engine/polygonClip.ts` — the rest of the app imports a normal, correctly-typed `intersectPolygons(a, b)` function and never touches the raw package's mismatched shape directly.

**What shipped:**
- **New `src/engine/settlementLayout.ts`** (pure, `Rng`-injectable, mirrors `dungeonLayout.ts`'s role for settlements):
  - `buildCityMask(districtCount, rng)` — 20 angular samples around a circle, each with a randomized radius (0.7-1.05× a base radius that scales with `sqrt(districtCount)`), returns a closed ring.
  - `sampleDistrictSites(count, mask, rng)` — Poisson-disc-like rejection sampling: places `count` points strictly inside the mask with a minimum pairwise distance, relaxing that minimum after repeated failures, with a final any-in-mask-point fallback — bounded attempts, always terminates with exactly `count` points (same "bounded retry, guaranteed termination" shape as `gridLayout.ts`'s anchor-retry fallback). Uses a hand-written ray-casting point-in-polygon test (simple enough not to need a dependency for it).
  - `buildVoronoiDistricts(sites, mask)` — one `d3-delaunay` Voronoi cell per site, each clipped to the mask via `intersectPolygons`; if clipping ever yields multiple disjoint pieces (pathological case), keeps the largest by area.
  - `buildRoadEdges(sites, seatIndex)` — pure function of site positions (no rng needed): the seat connects to its 2 nearest neighbors (main), non-seat sites form an angular ring loop around the seat when ≥3 exist (main), every site also gets a nearest-neighbor edge (minor) so no district is ever isolated.
- **`generateSettlement.ts`**: `District.cell`/`parentDistrictId` replaced with `District.site: Point` (the original generator point, used for road anchors and labels) and `District.polygon: Point[]` (the clipped Voronoi cell). `Settlement` gained `mask: Point[]` and `roads: {a, b, kind}[]` (district-id pairs, mirroring `DungeonSite.connections`).
- **New `src/hexgrid/SettlementMapSvg.tsx`** (parallel to `DungeonMapSvg.tsx`; supersedes `GridLayoutSvg.tsx` for settlements specifically — dungeons still use their own rectangle-based renderer from phase 5, and `GridLayoutSvg.tsx` itself is now settlement-districts-only until this phase, retired here in favor of the new one). Renders the mask as a background boundary polygon, each district as its real clipped polygon, and roads as SVG `<path>` quadratic-Bezier curves — the curve's control point is a fixed perpendicular offset from the edge midpoint (proportional to edge length), so no extra randomness or curve library is needed; the browser's native path renderer does the actual curve drawing.
- **`SettlementView.tsx`** updated to build the new renderer's props from `site.mask`/`site.districts`/`site.roads`; district click-to-expand and Reroll Site both preserved unchanged.
- **Deliberately deferred** (matching phase 5's precedent of dropping shadowdark-rest's heavier/buggier features): building footprints/plazas — shadowdark-rest's own `AGENTS.md` documents this as a capped-attempt rejection sampler with no fallback that silently underfills dense districts; not worth porting as-is, and a real fix is a separate, self-contained feature addable later without any schema change (districts already have real polygons to place buildings inside).

**Verification**: new `settlementLayout.test.ts` (11 tests — mask closure/growth, site containment within the mask, one non-degenerate polygon per site, sites falling within their own district's bounds, full road-graph connectivity across sizes 3/4/6/8, seat always has a main road) + rewritten `generateSettlement.test.ts` (4 tests, migrated from exact-scripted-rng values to seeded-rng + structural invariants, since rejection sampling has a variable, non-scriptable rng call count — same testing-philosophy shift `gridLayout.test.ts` already established). All 189 Vitest tests pass, `npx tsc -b`/`npm run build` clean (bundle grew from ~288KB to ~332KB, ~110KB gzipped). Browser-verified via Playwright: a generated 4-district Town rendered 4 real district polygons with 4 distinct vertex counts (8/9/10/11 — confirming genuine irregular Voronoi shapes, not uniform cells) connected by 5 curved roads (all using the SVG `Q` quadratic-Bezier command), district click-to-expand and Reroll Site both still work correctly, zero console errors. A screenshot of a 6-district city confirmed the visual result: an organic non-circular town boundary, irregular polygon districts tiling it, and curved roads connecting district centers.

### Revision: v1 looked like a diagram, not a map (same day, 2026-07-04)

**What went wrong:** the user reviewed a live 3-district Village and rejected it outright — flat solid-colored pie-wedge district shapes with no visible internal detail, reading as an abstract stained-glass diagram rather than a city. Root-caused two separate things: (1) a real bug — road `<path>`s were drawn *underneath* the fully-opaque district polygons, and since districts tile the entire mask with no gaps, the roads were almost completely hidden (the exact same z-order mistake as the Phase 5 dungeon-corridor visibility issue, never revisited); (2) a genuine scope gap — flat district polygons alone have no texture or density cues, which is exactly what the deferred "building footprints/plazas" feature would have provided.

**Reference alignment**: the user provided a concrete visual target — [thealexandrian.net's city map](https://www.thealexandrian.net/images/20180723c.jpg), a professionally painted map with dense organic building blocks, winding visible streets, green parks with tree icons, and a towered city wall. Fetched and viewed directly to confirm the target before rebuilding. Painted-texture fidelity (roof shingle patterns, stone wall texture, water shading) was explicitly scoped **out** as a much bigger lift (would need a texture-asset pipeline, closer to how `shadowdark-rest`'s hex map renderer uses real PNG textures) — user confirmed a flat/stylized SVG version in the *same visual language* was the right target for now. User also confirmed treating this as a Phase 7 revision, finishing it before Phase 8 (settlement NPC population), rather than deferring further.

**What shipped (un-deferring the building-footprint work, scoped to plain shapes, no new geometry dependency):**
- **`settlementLayout.ts` gained `generateBuildingFootprints(districtPolygon, mask, roadSegments, rng)`** — rejection-sampled rectangles (occasionally parks instead, ~10% chance) inside a district, avoiding the outer wall and roads by a minimum clearance. Distance-based avoidance via a hand-written point-to-segment-distance check, not true polygon buffering/offsetting — deliberately avoided needing a third geometry dependency (e.g. `turf.js`'s buffer) for this. **Density-based, not count-based**, on purpose: runs a bounded number of attempts proportional to district area and keeps whatever fits, rather than promising an exact count and silently falling short — the specific documented bug in `shadowdark-rest`'s own equivalent feature.
- **`generateSettlement.ts`**: `District` gained `buildings: BuildingFootprint[]`, generated in a pass after roads are computed (buildings need to know where roads are to avoid them).
- **`SettlementMapSvg.tsx` fully rewritten** with corrected z-order and the new visual language: ground fill → faint per-district color wash (opacity 0.16, orientation only — district *type* identity, not building color) → streets (thick cream ribbons, drawn **before** buildings so they stay visible) → building rectangles (small, rotated, cycling through a 4-color warm terracotta palette for texture) and park ellipses (green, with 3 small tree-dot markers each) → invisible district click-hit-regions (kept on top for reliable interaction) → wall (thick stroke along the mask) + towers (circles at every 3rd mask vertex — all of them was too dense) → labels (last, with a dark text-stroke halo for readability against the busier background now).
- **Verification**: 4 new tests in `settlementLayout.test.ts` (footprints placed, all inside their district's bounds, no two overlap, produces buildings). All 182 Vitest tests pass, `npx tsc -b`/`npm run build` clean (bundle ~334KB, ~111KB gzipped). Browser + screenshot verified via Playwright at both scales: an 8-district Metropolis rendered 135 buildings and 21 parks across its districts with 14 visible streets and zero console errors; a 3-district Village screenshot confirmed the style holds up at small scale too. District click-to-expand (now via the transparent hit-region polygon) and Reroll Site both still work.

## Location Generator expansion

**Confirmed requirement, 2026-07-04 — in progress.** A house-rule expansion (explicitly not book RAW — layered on top of verified RAW mechanics, never silently deviating from RAW without discussion), inserted as Phase 8, before Settlement NPC population and Mongo.

**Why:** the overland Points of Interest table (`src/data/tables.ts`) has 20+ distinct location flavors (towers, shrines, camps, rifts/anomalies, ruins, etc.), but `generateSiteForHex` only ever dispatches to a settlement or one of 4 generic dungeon Site Types (Cave/Tomb/Deep tunnels/Ruins) — most POIs resolve to a mechanically-identical dungeon with no thematic connection to their own flavor text. The fix has two parts: a richer overland POI table (a d200 "Location Generator": Terrain 2d6 → Feature d200 → conditional sub-tables, plus terrain-keyed Cataclysm d8×7 and Natural Landmark tables), and **5 new site kinds** so the dungeon-dispatch side actually has somewhere thematically appropriate to route those new POI flavors: **Tower, Shrine, Rift, Keep, Camp** (on top of the existing Settlement/Cave/Tomb/Deep-Tunnels/Ruins, for 10 total). Also confirmed: Village/Town/City/Metropolis POI labels should eventually bias/force the actual Settlement Type roll when reached, rather than the Settlement Type being rolled with no regard to the label that led there.

**Explicit sequencing decision:** build and verify each of the 5 new site kinds **in isolation** — unit tests, plus the same temporary-hack-then-revert Playwright pattern used throughout this plan (force `generateSiteForHex` to return the new kind, screenshot, confirm zero console errors, revert the hack) — before touching the POI table or `generateSiteForHex`'s dispatch logic at all. The full POI table rewrite and dispatch cutover (routing all 10 site kinds, plus the settlement-type-forcing behavior) is deliberately batched until all 5 new kinds exist, to avoid rewiring dispatch logic five separate times. Confirmed build order: **Tower → Shrine → Rift → Keep → Camp**, stopping to check in after each before starting the next.

**Status:**
- **Tower — done.** `src/engine/generateTower.ts`: level count is derived from the existing Size d6 roll (`towerLevelRangeForSize` in `dungeonTables.ts`, same "reuse Size, don't invent a second axis" pattern used everywhere in this phase), remapped to a level-count range instead of a room-count range. Ground floor is a fixed entry hall + guard room; levels above form a strictly linear chain (unlike a dungeon's open room graph). The top level's objective is **hard-coded**, not the normal highest-roll-wins rule — a deliberate exception, because a strictly linear chain has an "anticlimactic long walk" failure mode (the objective could roll onto level 2 of 6, making the rest of the climb pointless) that a hub-and-spoke or open graph doesn't have. `src/hexgrid/TowerMapSvg.tsx` renders a vertical elevation view. `src/components/hexdetail/TowerSiteView.tsx`. 8 tests in `generateTower.test.ts`. Browser-verified via the temporary-hack-then-revert Playwright pattern.
- **Shrine — done.** `src/data/shrineTables.ts`: full d6 Disposition table and full d6 Approach Feature table, both transcribed verbatim from the user (not invented) — duplicates across features are allowed, no dedup logic. `src/engine/generateShrine.ts`: rolls a d3 feature count; the core/shrine-itself feature is always the objective (Shrine has no room topology at all, so there's no "which room" question the way Tower/Keep/dungeons have). `src/components/hexdetail/ShrineSiteView.tsx` — no SVG renderer; rendered as a scene-card list instead, since there's nothing spatial to lay out. 3 tests in `generateShrine.test.ts` (a 4th, a probabilistic 500-sequential-seed search for a duplicate-feature case, was removed as flaky — the seeded LCG's first roll correlates near-linearly for small sequential seeds, so the search never found a hit; the scripted exact-value test already proves the "duplicates survive, no dedup" property deterministically). Browser-verified.
- **Rift — done.** `src/data/riftTables.ts`: d6 Origin, d6 Effect, d4 Stability, all transcribed verbatim, all independent rolls with no cross-referencing. `src/engine/generateRift.ts`: exactly 3 rolls, no room/feature topology, no monster/NPC population, no objective flag needed (there's only one "thing" — the rift itself). `src/components/hexdetail/RiftSiteView.tsx`: a single indivisible `.objective-room`-styled block covering all 3 axes together, plus an effect-radius note. 3 tests in `generateRift.test.ts`. Browser-verified.
- **Keep — done, browser-verified.** `src/engine/keepLayout.ts`: pure radial hub-and-spoke geometry — a courtyard hub, named trope rooms (Hall/Barracks/Armory/Lord's Quarters, from `NAMED_SLOTS`) as first-hop spokes filled in priority order up to whatever the Size-derived room count (`keepRoomCountRangeForSize` in `dungeonTables.ts`) provides (a Small keep, 3-4 rooms, may not get all 4 named slots), any additional rooms as generic second-hop overflow attached round-robin to a named parent — never more than 2 hops from the hub. A real overlap bug was found and fixed here during testing: fanning multiple generic rooms sharing one named parent by *global* sibling index repeated the same ±22° angle offsets after 2 siblings, causing an exact-position overlap for a 3rd/4th room on the same parent; fixed via per-parent sibling-index tracking with an increasing fan step, caught by `keepLayout.test.ts`'s explicit no-overlap-across-shared-parent case. `src/engine/roomContent.ts` gained `rollBiasedRoomContent` — a **"reroll-toward" bias, not a hard override**: roll normally, and only on a miss get exactly one more chance at the desired type(s), so a second full roll (with its own conditional detail/monster/npc sub-rolls) is only ever consumed when the first roll actually needs it. Used for Armory (biased toward Treasure) and Lord's Quarters (biased toward NPC/Boss Monster). `src/engine/generateKeep.ts`: `generateKeepSite` (courtyard is always room 1; named/generic fill as above; objective placement uses the **normal** highest-roll-wins rule, unlike Tower — Keep's hub-and-spoke shape doesn't have Tower's anticlimactic-walk failure mode, so no hard override is needed) and `generateKeepBasement` (an optional, GM-choice, ephemeral/re-rollable generator — not persisted onto `KeepSite` — that just calls `generateDungeonSite(rng, 'Deep tunnels')`, same pattern as the existing Tavern/Shop generators). `src/components/hexdetail/KeepSiteView.tsx` reuses `DungeonMapSvg` unmodified, since that renderer only cares about rects + connections, not the layout algorithm that produced them — no new renderer needed for Keep. Wired into `generateSite.ts`'s `GeneratedSite` union (type-only) and `HexDetailPage.tsx`'s route branch; `generateSiteForHex`'s actual dispatch logic is still unchanged (Settlement vs. Dungeon only), which is correct per the batched-cutover plan above, not a gap. 5/5 tests passing in `generateKeep.test.ts` (plus keepLayout.test.ts's 4/4) — one scripted test initially failed for a test-authoring reason, not a code bug: the script used roll=2 on the Room Type d10 table intending "Trap," but roll=2 actually maps to "Empty" (`roomTypeForD10` in `dungeonTables.ts` — Trap is roll=3); that single wrong intermediate value cascaded into a misaligned rng-consumption sequence for the rest of the scripted test. Fixed by correcting the scripted roll value. Full suite green: `npx tsc -b` clean, 223/223 Vitest tests passing. Browser-verified via the temporary-hack-then-revert Playwright pattern (localStorage seeded directly with a hex carrying a POI, bypassing the 1-in-6 book-RAW POI roll, since the goal was verifying rendering, not exercising the POI roll itself): a Small keep rendered as a courtyard hub (marked with the objective star) with 3 named spokes (Hall/Armory/Barracks, each labeled by initial) connected by visible corridors, no overlaps, correct room-type coloring, room list matching the SVG; zero console errors. Hack reverted immediately after, confirmed via `git diff` showing zero delta from the committed state.
- **Camp — done, browser-verified.** The scope-creep risk of the 5, flagged by the user in advance, since it needs a genuinely new "scatter/zone" renderer — Camp's spec is a flat, unordered cluster of a central feature plus N peripheral features with **no topology or connections at all**, which doesn't fit the rect+connections model every other renderer here assumes (Tower's vertical chain, Keep's hub-and-spoke, dungeons' open graph, settlements' Voronoi cells).
  - `src/data/campTables.ts`: `campFeatureCountRangeForSize` (Size → **total** feature count, central included — unlike Keep's explicit courtyard exclusion, Camp's Size table was given without that exclusion, so `peripheralCount = totalFeatureCount - 1`) and the d6 Peripheral Feature table (Sleeping area/Supply cache/Watch post/Mounts pen/Prisoner pit/Ritual-leader's-space), transcribed verbatim from the user.
  - `src/engine/campLayout.ts` — the **third distinct layout shape** in this project (after dungeons' BSP tiling/Keep's hub-and-spoke rects, and Tower's linear chain): `computeCampLayout(peripheralCount)` places a central circle at the origin and peripheral circles at evenly-spaced angles with **deterministic alternating radius jitter** (not rng-driven — same "organic-looking but fully reproducible" approach as `keepLayout.ts`'s fan angles, avoiding extra rng calls that would need to be accounted for in scripted tests). No connections/edges of any kind, by design.
  - `src/engine/generateCamp.ts` — `generateCampSite`: rolls Size, then total feature count within its range, then one Peripheral Feature roll per peripheral slot. **No monster/NPC engine attachment** (unlike dungeon/Tower/Keep rooms) — threats are embedded as flavor text directly in the table (e.g. "Watch post: armed NPCs"), same choice already made for Shrine. **No Danger Level roll** — not specified for Camp, consistent with Shrine/Rift also lacking one; not added without being asked. Objective placement is a **soft fallback** (looser than Tower's hard override, same shape as Keep's bias): whichever peripheral feature rolls "Ritual/leader's space" (first occurrence) becomes the objective; if none rolled, the central feature is the objective by default. The central feature's specific identity (campfire/war-tent/totem) is left as a generic placeholder — no formal table for it was given, only illustrative examples; real flavor is deferred to the POI-table cutover.
  - New `src/hexgrid/CampMapSvg.tsx` — plain circles at their computed positions, **no connecting lines drawn at all** (per spec: "a camp isn't traversed room-to-room"), deliberately simpler visual treatment than `DungeonMapSvg`/`SettlementMapSvg` (flat background, no patterns/textures) given the scope-creep flag. Uses a new `data-feature-id` Playwright-targeting attribute (a third convention alongside `data-room-id`/`data-district-id`, since "feature" fit better than "room" for a topology-free scatter). New `src/components/hexdetail/CampSiteView.tsx` and `CAMP_CENTRAL_COLOR`/`CAMP_FEATURE_COLORS` in `siteColors.ts`.
  - Wired into `generateSite.ts`'s `GeneratedSite` union (type-only) and `HexDetailPage.tsx`'s route branch; dispatch itself is still unchanged, correct per the batched-cutover plan.
  - **Verification**: 9 new tests (5 in `campLayout.test.ts` — correct peripheral counts, non-negative coordinates, no circle-overlap across counts, no `connections` property at all; 4 in `generateCamp.test.ts` — scripted Ritual-feature-becomes-objective and central-fallback-objective cases, plus seeded structural invariants across 6 seeds confirming valid feature counts and exactly one objective per site). Full suite green: `npx tsc -b`/`npm run build` clean, 232/232 Vitest tests passing. Browser-verified via the temporary-hack-then-revert Playwright pattern (same `localStorage`-seeding approach as Keep's verification): a Large camp rendered as 6 peripheral circles at irregular distances around a central circle, correctly colored by feature type, no connecting lines, objective star correctly placed on the central feature (no Ritual/leader's space rolled that generation); zero console errors. Hack reverted immediately after, confirmed via `git diff` showing zero delta from the committed state.
- **POI table cutover — done, browser-verified (2026-07-05).** New `src/data/locationTables.ts`: `SiteKind` type, range-expanded 200-entry Feature table (`locationFeatureForD200`) transcribed verbatim from the locked table below, terrain-keyed Cataclysm d8×7 (`cataclysmForTerrain`) and Natural Landmark (`naturalLandmarkForTerrain`) tables. `src/data/tables.ts`'s old d20 `POINTS_OF_INTEREST`/`pointOfInterestForD20`/`CATACLYSM_TRIGGER_DEVELOPMENT`/flat `CATACLYSMS`/`cataclysmForD8` removed as dead code (fully superseded); `SettlementColumn`/`SETTLEMENT_NAMES`/`SETTLEMENT_LOCATIONS` split from 3 columns (combined "City/metropolis") to 4 (Village/Town/City/Metropolis — 8 new invented Metropolis names added, since City/Metropolis are now distinguished by the Feature table).
  `PointOfInterest` (`generateHex.ts`) gained `siteKind?: SiteKind | 'none'`, `forcedType?: string`, `naturalLandmark?: string`. `rollPointOfInterest` now takes `terrain` (reusing the hex's own already-rolled terrain, not a fresh 2d6, per the plan's Step 1 note) and rolls d200 instead of d20: Cataclysm/Natural landmark rows resolve to `siteKind: 'none'` with terrain-keyed flavor text and no further site generation; every other row sets `siteKind` + `forcedType` (+ a flavor tag stored in `development` where the table specifies one).
  `generateSiteForHex` (`generateSite.ts`) now switches directly on `poi.siteKind`: `'none'` → `undefined` (no site); a real `SiteKind` → dispatches to that generator, passing `poi.forcedType` through as the overridden Type roll for `'dungeon'`/`'settlement'` (the other 5 kinds have no Type to force) — this is how Village/Town/City/Metropolis POI labels now force the Settlement Type roll, confirmed working via `generateSite.test.ts` and a live Metropolis POI reaching Castle District (only possible via Metropolis's d8 roll) in browser verification. `siteKind` left `undefined` (manually-edited hexes via the free-text Edit form) falls back to the pre-cutover `SETTLEMENT_LOCATIONS`-based heuristic unchanged.
  `HexDetailPage.tsx` no longer auto-dispatches `GENERATE_SITE` when `poi.siteKind === 'none'` (would otherwise never settle `hex.site` and re-render forever) and routes that case to `WildernessView` (tweaked to show "a notable feature, but nothing here to delve into" instead of the plain-wilderness note when a POI is present). `HexBaseInfo.tsx` gained a `naturalLandmark` display line; `development` is now only rendered when non-empty.
  New `locationTables.test.ts` (49 tests: full 1–200 coverage + boundary spot-checks against the locked table, cataclysm/landmark structural + spot-checks). `generateHex.test.ts`/`generateSite.test.ts`/`mapReducer.test.ts` updated for the new terrain-param signature, d200 rolls, and forced-type dispatch (old d20/flat-cataclysm tests deleted as dead). Full suite green: 290/290 Vitest tests, `npx tsc -b`/`npm run build` clean. Browser-verified via a direct `localStorage`-seeded Playwright pass (no source-code hack needed this time — dispatch is now the real production path) over 4 representative rows: a Cataclysm POI and a Natural landmark POI both showed terrain-keyed flavor text with no site and no "Reroll Site" button; a "Small tower" POI rendered a real Tower site; a Metropolis-forced POI rendered an 8-district settlement reaching Castle District. Zero console errors across all 4.

### Locked table content (verbatim — this is the durable copy; nowhere else preserves it)

**Earlier in this session the full table content below was only ever pasted into chat, never written to a file — losing it once already required manually mining the raw conversation transcript to recover it. Do not let that happen again: this section is the single source of truth for the Location Generator's table data. If it's ever changed, edit it here first.**

#### Step 1: Terrain (2d6)

Identical to the existing Hex Terrain 2d6 table (`terrainFor2d6` in `src/data/tables.ts`) — this step is **not** a fresh roll when integrated into per-hex POI generation; it reuses the hex's own already-rolled `terrain` field to key the Cataclysm/Natural Landmark sub-tables below. (Reproduced here only to show why the two tables are identical, confirming that reuse is correct rather than coincidental.)

| Roll | Terrain |
|---|---|
| 2 | Desert/Arctic |
| 3 | Swamp |
| 4–6 | Grassland |
| 7–8 | Forest/Jungle |
| 9–10 | River/Coast |
| 11 | Ocean |
| 12 | Mountain |

#### Step 2: Feature (d200) — locked, with final routing

Every row now has an explicit routing (confirmed across several messages resolving the initial "judgment calls" — see history below the table for provenance of the less-obvious calls). "Forced" means the site generator's own Size/Type/Settlement-Type roll is overridden to this value; a blank Forced Type for the generic "Dungeon" row means it still rolls its own Site Type fresh (today's existing behavior, unchanged). "Flavor" is a display-only tag surfaced to the GM (confirmed decision: **flavor-as-data, not flavor-as-generation** — stored and shown, but not wired into room-content/monster tables yet, to avoid coupling this structural cutover to unrelated content work).

| Roll | Feature | Routes to | Forced Type | Flavor |
|---|---|---|---|---|
| 1 | Cataclysm | *(no site — terrain-keyed Cataclysm text only, see Step 3a)* | — | — |
| 2–13 | Small tower | Tower | — | — |
| 14–19 | Fortified keep | Keep | — | — |
| 20–24 | Large tomb | Dungeon | Tomb | "Large tomb" |
| 25–30 | Tomb | Dungeon | Tomb | — |
| 31–40 | Dungeon | Dungeon | *(fresh roll, unchanged)* | — |
| 41–46 | Deep tunnels | Dungeon | Deep tunnels | — |
| 47–54 | Cave | Dungeon | Cave | — |
| 55–60 | Mine (active) | Dungeon | Deep tunnels | "Active mine" |
| 61–66 | Mine (abandoned) | Dungeon | Deep tunnels | "Abandoned mine" |
| 67–70 | Sinkhole/chasm | Dungeon | Cave | "Sinkhole/chasm (vertical drop)" |
| 71–83 | Natural landmark | *(no site — terrain-keyed landmark text only, see Step 3b)* | — | — |
| 84–87 | Temple (oracle) | Shrine | — | — |
| 88–93 | Ruins (temple) | Dungeon | Ruins | "Temple ruins" |
| 94–99 | Barrow mounds | Dungeon | Tomb | "Barrow mounds" |
| 100–103 | Necropolis/catacombs | Dungeon | Tomb | "Necropolis/catacombs" |
| 104–105 | Sunken ruins | Dungeon | Ruins | "Flooded" |
| 106–113 | Village (ruins) | Settlement | Village | POI development text: "Abandoned and in ruins" |
| 114–117 | Town (siege) | Settlement | Town | POI development text: "Under siege" |
| 118–121 | Ravine (cult) | Dungeon | Cave | "Ravine — religious cult lair" |
| 122–125 | Goblin/orc warren | Dungeon | Deep tunnels | "Goblin/orc warren" |
| 126–129 | Bandit hideout | Camp | — | — *(default; no terrain-keyed Cave-hideout exception in v1 — confirmed, revisit only if playtesting wants it)* |
| 130 | Monster nest | Dungeon | Cave | "Monster nest" |
| 131 | Hermit's abode | Shrine | — | — |
| 132 | Cave formation | Dungeon | Cave | "Cave formation" |
| 133 | Ancient dolmens | Dungeon | Tomb | "Ancient dolmens — guardian" |
| 134 | Barbarian camp | Camp | — | — |
| 135 | Holy shrine (door to another plane) | **Rift** | — | — *(confirmed explicitly: the distinguishing text is "door to another plane," so this is the portal case, not the lightweight-NPC Shrine case)* |
| 136–137 | Dragon's lair | Dungeon | Cave | "Dragon's lair" |
| 138–139 | Planar rift/portal | Rift | — | — |
| 140–141 | Lost library/arcane archive | Dungeon | Ruins | "Arcane archive" |
| 142–174 | Village | Settlement | Village | — |
| 175–193 | Town | Settlement | Town | — |
| 194–199 | City | Settlement | City | — |
| 200 | Metropolis | Settlement | Metropolis | — |

Ranges sum to exactly 200 with no gaps or overlaps (verified by hand when transcribing this table — re-verify with a unit test when implementing, e.g. every integer 1–200 resolves to exactly one feature).

#### Step 3a: Cataclysm (d8, keyed to terrain)

| Roll | Desert/Arctic | Swamp | Grassland | Forest/Jungle | River/Coast | Ocean | Mountain |
|---|---|---|---|---|---|---|---|
| 1 | Ash-buried dunes / fire-mountain melting ice | Ash poisoned the wetland waters | Distant eruption blanketed the plains in ash | Ash buried the canopy, killing old growth | Ashfall poisoned the water, killed the fish | Undersea eruption birthed (or sank) an island | Eruption reshaped peaks, buried valleys |
| 2 | Wildfire raced through scrub / firestorm melted permafrost | Peat fire smoldered underground for years | Wildfire consumed leagues of open grass | Great burning reduced ancient trees to char | Burning swept a coastal town or delta reeds | A burning fleet, or fire that burns on water | Firestorm sparked by lava or lightning |
| 3 | Shifting dunes swallowed an oasis / ice shelves collapsed | Villages sunk into the mire | Fissures split the plains, swallowing herds and homes | Toppled forests, opened sinkholes | Quake rerouted the river or sank the coastline | Seaquake triggered a tsunami | Quake leveled cliffside settlements |
| 4 | Sandstorm/blizzard buried a settlement in days | Hurricane-driven surge drowned the lowlands | Supernatural tempest or endless tornado outbreak | Hurricane or magical gale flattened the canopy | Hurricane or tidal storm reshaped the shore | Legendary hurricane sank a fleet or coastline | Avalanche-triggering blizzard or lightning storm |
| 5 | Flash flood through a dry wash / glacial melt | Swamp overflowed its banks and never receded | River burst its banks and remade the land | Monsoon or dam-break drowned the undergrowth | River drowned everything along its banks | Rising seas swallowed islands or ports | Burst dam or glacial lake flooded the valley |
| 6 | Bones of a battle still bleach in sun/snow | Armies bogged down and slaughtered in the mud | A decisive battlefield, now littered with ruins | Battle scarred the woods with old fortifications | Naval battle or siege whose wreckage lines the shore | Great naval battle, wrecks litter the seabed | Siege fought over a mountain pass or fortress |
| 7 | Plague spread along a caravan route | Disease bred in stagnant, fetid water | Blight killed livestock and herders alike | Blight or fungal rot spread tree to tree | Disease carried in on ships or fishing communities | Plague ship or cursed contagion spread by sailors | Plague spread through isolated mining communities |
| 8 | Spell turned fertile land to endless sand/ice | Ritual corrupted the water and life within it | Spell summoned endless storms or twisted wildlife | Wild magic twisted the flora and fauna | Ritual summoned a tsunami or cursed the waters | Ritual summoned a kraken/maelstrom, drowned a civilization | Ritual awoke something in the depths, cursed the stone |

#### Step 3b: Natural Landmark (keyed to terrain, no roll — one fixed entry per terrain)

| Terrain | Landmark |
|---|---|
| Desert/Arctic | Field of towering rock spires (or ice pinnacles) carved by wind, hiding ancient petroglyphs |
| Swamp | Massive, hollowed-out cypress tree wide enough to serve as shelter, rising from the mire |
| Grassland | Lone standing stone circle, weathered and half-sunken into the earth |
| Forest/Jungle | Colossal "mother tree," centuries old, roots forming natural tunnels and archways |
| River/Coast | Natural rock arch or sea cave carved by centuries of tidal erosion |
| Ocean | Ring of jagged sea stacks marking a sunken reef or old wreck |
| Mountain | Natural amphitheater or crater lake in a dormant volcanic caldera |

#### How to use it (book-style summary, for GM reference — not literally how the code should be structured)
1. Roll 2d6 for terrain (in practice: reuse the hex's own terrain, already rolled — see Step 1 note above).
2. Roll d200 for the feature.
3. If Cataclysm (1) or Natural landmark (71–83), roll/look up the matching terrain-keyed sub-table.
4. Everything else: route to the site kind per the table above, with the given Forced Type/Flavor.

#### Provenance of the less-obvious routing calls (for anyone auditing this later)
- **Mine (active/abandoned) → flavor of Deep Tunnels, not a new kind.** Same shape; "active" swaps in worker NPCs/mundane hazards, "abandoned" swaps in collapse hazards/monster squatters (not yet wired — flavor-as-data only, see above).
- **Sinkhole/chasm → flavor of Cave.** A vertical-drop variant — same generation logic, different entrance/room framing.
- **Sunken ruins → flavor of Ruins ("flooded").** Add swimming/tide hazards to room connections later — no new kind needed now.
- **Ravine / Monster nest / Goblin-orc warren / Dragon's lair → flavor of Cave or Deep Tunnels** (monster-lair population variants on existing geometry) — resolved individually above: Ravine/Monster nest/Dragon's lair → Cave (open/natural reading); Goblin/orc warren → Deep tunnels (excavated/warren-like reading).
- **Bandit hideout → Camp by default, no terrain-keyed exception in v1.** Explicitly confirmed: don't add a terrain-conditional branch this early; a GM wanting the Cave-lair version on Mountain terrain can override manually. Revisit only if playtesting shows people consistently want it.
- **Temple (oracle) / Hermit's abode → Shrine kind (lightweight).** These are about an NPC/interaction, not a delve — full dungeon generation is overkill.
- **Holy shrine (135) → Rift, not Shrine.** The distinguishing text is "door to another plane" specifically — the portal case, confirmed explicitly rather than assumed from the "shrine" name alone.
- **Ancient dolmens → Dungeon/Tomb with a "guardian" flavor**, not a bespoke Shrine-adjacent kind — resolved this session (2026-07-05) after being flagged as unruled; treated as a small Tomb-flavored delve guarding something, consistent with "protected by an age-old guardian."
- **Lost library/arcane archive → Dungeon/Ruins with an "arcane archive" flavor** — resolved this session (2026-07-05); a decayed structure holding old knowledge fit "Ruins" better than "Tomb" (sealed/buried) or "Deep tunnels" (excavated vault).
- **Barrow mounds / Necropolis-catacombs / Large tomb → Dungeon/Tomb**, each carrying its own name as a flavor tag rather than collapsing into one generic "Tomb" flavor — resolved this session (2026-07-05); no information from the original feature names is lost.

**Verification pattern used throughout**: same as every other phase in this doc — scripted-exact-value + seeded-structural Vitest tests, `npx tsc -b`/`npm run build` clean, and the temporary-hack-then-revert Playwright screenshot pattern (force `generateSiteForHex` to return the new kind, verify visually and check console errors, then revert before considering the kind done) rather than trusting typecheck/tests alone for anything touching new SVG rendering — consistent with this project's documented history of 3 silent visual bugs that passed typecheck/build cleanly.

## Settlement NPC population

**Confirmed requirement, 2026-07-04 — design not yet finalized.** Sequenced immediately after "Real settlement maps," before Mongo.

**Why:** settlement district POIs already exist (`generateSettlement.ts` rolls POIs per district), but like dungeon rooms, they're text-only today. Confirmed scope: dungeons + settlements only (not overland random encounters).

**Design sketch:**
- Reuses the Monster/NPC generation engine built in "Dungeon monster/NPC population," extended with settlement-specific flavor.
- `shadowdark-rest` keys settlement NPC generation off an `NpcQuality` model (`appearance`, `does`, `secret`, weighted `age`/`wealth`) combined with Name/Race/Personality/Background servers, matching Background to POI type by keyword (`SettlementServer.scala`, `NpcQualityRepository`/`NpcQualityServer`). **This specific code was not reviewed in detail during the initial map-generation research pass** (explicitly out of scope for that pass, which focused on map/terrain generation) — do a dedicated review of those files when this phase starts.
- Open scope question for phase start: whether every POI gets an NPC, or only certain POI types (e.g. a shop/tavern gets a named proprietor; a "cairn" or "ravine" POI likely doesn't need one).

## Neighbor-weighted terrain generation

**Design sketch below is more concrete than a placeholder now** (informed by reviewing `shadowdark-rest`'s hex map generator, which already solves an equivalent problem), but still not implemented. Sequenced last (after Mongo) since it's a self-contained change to `engine/generateHex.ts`'s pure functions with no schema/storage implications, so it doesn't need to block or be blocked by anything else.

- The book's actual mechanic (`rollNextTerrain`, the New Hex 2d6 table) only steps relative to the *one* hex the party moved from. The user wants a new hex's terrain to be influenced by *all* of its already-revealed neighbors instead, for a more geographically coherent map. This is a deliberate house-rule layered on top of RAW, not a bug fix — today's predecessor-relative stepping is correct per the book and should probably remain available (e.g. as a fallback when a hex has only one revealed neighbor).
- **Explicit requirement: Ocean must form sensible, contiguous bodies**, not isolated single hexes. Today `Ocean` sits between `River/coast` and `Mountain` in the circular `TERRAIN_ORDER`, so a plain step can drop a lone Ocean hex next to Mountain neighbors with no other water nearby — reads as geographically wrong.
- **Recommended approach, found by reviewing `shadowdark-rest`'s `HexMapServer.scala`** (which independently solves this same problem for its own hex maps):
  1. **Connected-component "keep largest cluster" trim.** Their `trimDisconnectedRivers` + `connectedComponents` (`HexMapServer.scala:412-472`) run a plain BFS/flood-fill over hex adjacency to find all River/coast clusters, keep only the largest, and downgrade every hex not in it back to land. This is a pure graph algorithm (no textures/rendering involved) that ports directly to TypeScript over our existing `neighborsOf` (`src/hexgrid/hexMath.ts`) and mechanically satisfies the contiguity requirement as a post-processing guarantee, regardless of whatever primary weighting logic picks the terrain.
  2. **Adjacency-gated Ocean placement.** Their `nextTerrainStepWithRules`/`allowOcean` (`HexMapServer.scala:271-289,137`) only lets a hex roll Ocean if a neighbor is already Ocean (propagation) or via a small random seed chance — never a bare unweighted step onto Ocean. Prevents most isolated-Ocean cases before they'd even need trimming.
  - **Important scaling caution — do not copy this part:** `shadowdark-rest`'s `nextMap` recomputes connectivity over its *entire* accumulated hex list on every single new hex added (`HexMapServer.scala:182`) — acceptable for their small bounded map bursts, but wrong for this project's now-unbounded map (Phase 4). Any port must scope recomputation to just the connected component touching the newly revealed hex, not the whole revealed set.
  - Lower priority, more general technique also present there: a neighbor-majority "poll all 6 neighbors, pick the plurality axis" pattern (`riverAxisForDirs`, `HexMapServer.scala:363-375`) generalizes to weighting ordinary (non-Ocean) terrain by all revealed neighbors, not just the one predecessor — worth doing but not the load-bearing fix for the Ocean requirement specifically.
  - **Edge-case policy to decide explicitly** (shadowdark-rest oscillated on this without settling it, per its own `AGENTS.md`): what happens when a freshly-revealed region has exactly one isolated water hex and no established coastline yet — strict "always trim" could wipe all water from a region that just hasn't been explored far enough to reveal its neighbors yet. Decide up front whether to special-case "allow exactly one isolated hex until N more neighbors are revealed" or accept the strict rule.

## Verification

- `npm run test` (Vitest) after each phase — new suites listed above must pass alongside existing 58 tests.
- `npx tsc -b` clean after each phase.
- Playwright pass (per CLAUDE.md's existing pattern: `require('playwright').chromium.launch(...)`, target via `data-hex-id`/`data-room-id`/`data-district-id`) after phases 2, 4, 5, 7, 8, and 10 specifically, since those touch rendering/navigation/async-loading — the categories that have broken silently before in this project. (Phases 2 and 4 already done and verified this way; 5/7 are the dungeon-renderer/settlement-renderer phases, already done; 8 (the new Location Generator site-kind renderers + POI table cutover) is done and browser-verified; 10 is the Mongo-async-load phase.)
- Phase 10 additionally: manual smoke test of save → reload → same nested site content round-trips correctly through MongoDB (start local `mongod` or point `MONGODB_URI` at an accessible instance).

## Open items to confirm before/while building (lower-stakes, decided pragmatically above but flagged)

- Exact `Trap`/`Monster Mob`/etc. two-column detail sub-tables need care transcribing (some book Room Type detail tables roll a d6 against two parallel "Detail" columns, not a single column) — handle case-by-case from the already-read page text during Phase 1.
- Production hosting/SPA-fallback rule for the router isn't addressed (no deploy target chosen yet) — not a blocker for local dev.
- Client/server TypeScript type sharing via relative type-only imports across the two `tsconfig.json`s should resolve cleanly but hasn't been hands-on verified in this exact repo layout — first thing to confirm in Phase 4, with type duplication as a fallback.
- **Deferred, not forgotten (2026-07-04):** avoiding exact-duplicate room detail text within one generated dungeon (e.g. two separate "Minor Hazard: Collapsing walls" rooms) was raised alongside the mundane/theming fixes above but explicitly not selected for that pass. Candidate approach if picked up later: reroll a detail once if it exactly matches an earlier room's detail string in the same `generateDungeonSite` call (bounded retry, same pattern as `gridLayout.ts`'s anchor-retry fallback) — book RAW doesn't forbid duplicates (rolls are independent), so this would be a pure UX/variety improvement layered on top, not a RAW correction.
